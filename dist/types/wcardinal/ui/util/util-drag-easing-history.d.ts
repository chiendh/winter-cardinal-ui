export declare class UtilDragEasingHistory {
    dx: number;
    dy: number;
    ds: number;
    dt: number;
    constructor(dx: number, dy: number, ds: number, dt: number);
    set(dx: number, dy: number, ds: number, dt: number): void;
}
