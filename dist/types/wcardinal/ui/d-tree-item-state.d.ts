/**
 * Tree item states.
 */
export declare const DTreeItemState: {
    /**
     * the parent item is expanded
     */
    EXPANDED: number;
    /**
     * the parent item is collapsed
     */
    COLLAPSED: number;
    /**
     * the child item is selected
     */
    SELECTED: number;
};
